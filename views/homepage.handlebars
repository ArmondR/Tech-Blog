<ol class="post-list">
    {{#each posts as |post|}}
    <li>
        <article class="post">
            <div class="title">
                <a href="/post/{{post.id}}">{{title}}</a>
                <span>posted by {{user.username}} on {{created_at}}</span>
            </div>
        </article>
    </li>
    {{/each}}
</ol>